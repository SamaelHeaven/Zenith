plugins {
    id "java-library"
    id "maven-publish"
    id "org.jetbrains.kotlin.jvm" version "1.5.31"
    id "org.openjfx.javafxplugin" version "0.0.10"
}

group "org.samaelheaven"
version "1.0-SNAPSHOT"

repositories {
    mavenCentral()
}

def javafxVersion = "17.0.1"

tasks.withType(JavaCompile).configureEach {
    options.encoding = "UTF-8"
}

kotlin {
    jvmToolchain {
        languageVersion = "17"
    }
}

javafx {
    version = javafxVersion
    modules = ["javafx.controls", "javafx.fxml"]
}

dependencies {
    for (os in ["win", "linux", "mac"]) {
        runtimeOnly "org.openjfx:javafx-base:$javafxVersion:$os"
        runtimeOnly "org.openjfx:javafx-graphics:$javafxVersion:$os" // Fixed typo in module name
        runtimeOnly "org.openjfx:javafx-controls:$javafxVersion:$os" // Added missing module
    }
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}